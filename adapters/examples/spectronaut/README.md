# Spectronaut Example

Protein groups are rows and samples are columns. The values are already log2
intensities, so no log transform is requested.

```bash
bash adapters/examples/spectronaut/run.sh
```
